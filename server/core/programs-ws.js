const { getAvailablePrograms } = require('./programs')

// Receive
// wss.receiveTopic("programs", async (ws, event, body) => {
//     wss.subscribe(ws, "programs")
// })
// wss.receiveEvent("programs", "roles", async (ws, body) => {
//     wss.sendEvent(ws, "programs", "roles", ROLES)
// })

// Send
